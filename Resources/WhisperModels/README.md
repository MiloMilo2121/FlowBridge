# Whisper model assets

Run:

```sh
./scripts/fetch-whisper-small.sh
```

The script stages model files into:

```text
Resources/WhisperModels/WhisperSmall
```

That folder is intentionally ignored by Git because the CoreML model bundle is large.

